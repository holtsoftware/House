<Page
	x:Class="Sannel.House.Control.Views.MainView"
	xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
	xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
	xmlns:local="using:Sannel.House.Control.Views"
	xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
	xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
	mc:Ignorable="d">

	<Grid Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">
		<Grid.RowDefinitions>
			<RowDefinition Height="auto" />
			<RowDefinition />
		</Grid.RowDefinitions>
		<Grid.ColumnDefinitions>
			<ColumnDefinition />
			<ColumnDefinition />
		</Grid.ColumnDefinitions>
		<StackPanel Grid.Column="0">
			<AppBarButton x:Name="HomeAction" Icon="Home" IsCompact="True" />
		</StackPanel>
		<StackPanel Grid.Column="1" HorizontalAlignment="Right" Orientation="Horizontal">
			<StackPanel>
				<TextBlock x:Name="Time" HorizontalAlignment="Center" />
				<TextBlock x:Name="Date" HorizontalAlignment="Center" />
			</StackPanel>
			<AppBarButton x:Name="SettingsAction" Icon="Setting" IsCompact="True" />
		</StackPanel>
		<ContentControl x:Name="ActiveItem" Grid.ColumnSpan="2" Grid.Row="1" Margin="24,0" />
	</Grid>
</Page>
